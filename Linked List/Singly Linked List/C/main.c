#include <stdio.h>
#include <stdlib.h>
#include "operations.h"

int main()
{
    // create linked list 
    create();
    
    // traverse 
    // traverse();
    
    // middleNode
    // printf("\n ELemenet ; %d" , middleNode()); 
    
    // start = reverse(start);
    
    traverse();
    deleteNode(3);    
    traverse();
    return 0;
}